version: '3.9'
services:

  postgres:

    image: postgres:16
    container_name: postgres_restaurante
    restart: always

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: restaurante
    ports:
      - "5432:5432"